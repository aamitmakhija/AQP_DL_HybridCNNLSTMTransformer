numpy>=1.26
pandas>=2.2
pyarrow>=16.0
tqdm>=4.66
PyYAML>=6.0
scikit-learn>=1.5
# Torch is installed per-platform:
#   CPU/MPS (mac):    pip install torch torchvision torchaudio
#   CUDA 12.1 (GPU):  pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio
